schema = "https://raw.githubusercontent.com/cloudflare/wrangler/main/packages/wrangler/config-schema.json"
name = "mpc-conect"                    # Cambiado para coincidir con tu Worker
main = "src/worker/index.ts"
compatibility_date = "2026-03-08"      # Fecha actualizada

# Secrets: GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, AUTH_TOKEN, GITHUB_TOKEN
# Deben configurarse via Cloudflare Dashboard o CLI: 'wrangler secret put'

[[kv_namespaces]]
binding = "MCP_CACHE"
id = "98a89168e6304961bd9503d68b029210" # Tu KV Namespace